return {
    Debug = {
        DEBUG_ON = false,
        OCTREE_BRICKCOLOR = BrickColor.random(),
    },
    Module = {
        DEFAULT_CAPACITY = 4,
        MIN_SIZE_STUDS = 1,
        EPSILON = 1e-3
    }
}